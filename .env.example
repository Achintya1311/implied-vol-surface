# No key is required. Both live sources (NSE's public option-chain
# endpoint, yfinance) are free and unauthenticated.
#
# NSE's edge blocks unfamiliar request patterns regardless of headers or
# cookies - see README limitations. There is nothing to configure around
# that; the fixture path (the default) is unaffected.
